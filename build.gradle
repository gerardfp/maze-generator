plugins {
    id 'java'
    id 'maven-publish'
}

group 'com.github.gerardfp'
version '5.0'

publishing {
    publications {
        gpr(MavenPublication) {
            artifactId = "maze-generator"
            from(components.java)
        }
    }
}
